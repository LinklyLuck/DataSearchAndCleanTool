# reuqire.txt — minimal + safe superset to run app.py demo
# Core UI
streamlit>=1.38

# Dataframes (pandas for minimal demo; polars if you switch回the_pipeline_v2)
pandas>=2.2
numpy>=1.26
pyarrow>=15
polars>=1.5

# File formats
openpyxl>=3.1
xlsxwriter>=3.2

# HTTP & progress (providers / async utils / logs)
httpx>=0.27
tqdm>=4.66

# Config / APIs
pyyaml>=6.0
kaggle>=1.6
python-dotenv>=1.0

# Optional: render .dot graphs from results if需要（python层）
graphviz>=0.20

